"""
Integration tests for Synthesis Math Tutor

These tests require a running instance of the application.
Run the backend server before executing these tests:
    cd backend && uvicorn app.main:app --reload

Or use the test runner script:
    python backend/tests/integration/run_integration_tests.py
"""
